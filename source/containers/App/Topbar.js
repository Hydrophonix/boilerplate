// Core
import React from 'react';

import { TopbarContainer } from './styles';

export const Topbar = () => {
    return (
        <TopbarContainer>
            TOPBAR
        </TopbarContainer>
    );
};
